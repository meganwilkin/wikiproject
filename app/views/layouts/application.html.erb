<!DOCTYPE html>
<html>
<head>
  <title>Wikiproject</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
     <ul class="nav nav-tabs">
        <li><%= link_to "Home", root_path %><li>
        <li><%= link_to "About", about_path %><li>
      </ul>

      <% if flash[:notice] %>
       <div class="alert alert-success">
         <button type="button" class="close" data-dismiss="alert">&times;</button>
         <%= flash[:notice] %>
       </div>
     <% elsif flash[:alert] %>
       <div class="alert alert-warning">
         <button type="button" class="close" data-dismiss="alert">&times;</button>
         <%= flash[:alert] %>
       </div>
     <% end %>
      <div class="jumbotron">
        <h1>Wikipedia</h1>
        <% if user_signed_in? %>
          <h3>Welcome back to Wikipedia <%= current_user.username %></h3>
           Signed in as: <strong><%= current_user.email %></strong> |
           <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
         <% else %>
          <h3>Wikipedia is a resource for creation and sharing Wiki's</h3>
          <p>You must sign in or sign up before you can access Wikipedia<p>
            <div class="row-fluid">
              <div class="col-md-6">
              <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-success" %>
              </div>
              <div class="col-md-6">
              <%= link_to "Sign In", new_user_session_path, :class => "btn btn-success" %>
              </div>
            </div>
          <% end %>

          <%= yield %>
        </div>
  </div>
</body>
</html>
